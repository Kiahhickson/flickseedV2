<p id="notice"><%= notice %></p>

<h1>Listing Swipes</h1>

<table>
  <thead>
    <tr>
      <th>Founder</th>
      <th>Investor</th>
      <th>Favourite</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @swipes.each do |swipe| %>
      <tr>
        <td><%= swipe.founder_id %></td>
        <td><%= swipe.investor_id %></td>
        <td><%= swipe.favourite %></td>
        <td><%= link_to 'Show', swipe %></td>
        <td><%= link_to 'Edit', edit_swipe_path(swipe) %></td>
        <td><%= link_to 'Destroy', swipe, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Swipe', new_swipe_path %>
